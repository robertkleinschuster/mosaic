<?php

namespace Mosaic;

/**
 * marks an enum as renderable
 * class attributes and enum case attributes will be rendered
 * @see RenderableBackedEnum to enable rendering of enum values
 */
interface RenderableEnum
{
}
